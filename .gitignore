/node_modules
/public/hot
/public/storage
/storage/*.key
/vendor
.env
.env.backup
.phpunit.result.cache
docker-compose.override.yml
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/.idea
/.vscode
/.claude
error_log

# Dated project backups kept next to the code (11aug.zip, aug10.zip ...).
# Only the repo root - the zips shipped inside public/ and resources/ are
# part of the app.
/*.zip
